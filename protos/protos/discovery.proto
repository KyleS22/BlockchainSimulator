syntax="proto3";

message DiscoveryMessage{

    enum MessageType{
        DISCOVERY = 0;
        CONNECT = 1;
    }

    MessageType message_type = 1;
    string ip_address = 2;
    int32 port = 3;
}